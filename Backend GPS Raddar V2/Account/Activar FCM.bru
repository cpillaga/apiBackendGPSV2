meta {
  name: Activar FCM
  type: http
  seq: 2
}

patch {
  url: {{url_backend}}/api/v1/accounts/activateFCMAccount?isactivate=true
  body: none
  auth: bearer
}

params:query {
  isactivate: true
}

auth:bearer {
  token: {{Authorization}}
}

settings {
  encodeUrl: true
  timeout: 0
}
